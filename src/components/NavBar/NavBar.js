import React from "react";
import styles from "./NavBar.module.css";

const NavBar = () => {
  return (
    <div className={styles.container}>
      <h1>LOGO</h1>
      <button>Add hotdog</button>
    </div>
  );
};

export default NavBar;
